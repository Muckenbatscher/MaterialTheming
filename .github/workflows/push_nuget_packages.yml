name: Publish NuGet packages

on:
  release:
    types:
      - published
      
env:
  PACKAGES_ARTIFACT: nuget-package-${{ github.event.release.tag_name }}

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
    - name: Check Version Format in Tag
      uses: matt-usurp/validate-semver@v3
      id: version
      with:
        version: ${{ github.event.release.tag_name }}
        
  build-package:
    needs: version 
    runs-on: windows-latest
    
    strategy:
      matrix:
        project: 
          - name: MaterialTheming
          - name: MaterialTheming.Winforms
          - name: MaterialTheming.Wpf
          - name: MaterialTheming.MudBlazor

    env:
      VERSION: ${{ needs.version.outputs.version }} 
      OUTDIR: 'PackageBuild'

    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x

    - name: Restore project dependencies
      run: dotnet restore ${{ matrix.project.name }}/${{ matrix.project.name }}.csproj

    - name: Build project
      shell: pwsh
      run: dotnet build ${{ matrix.project.name }}/${{ matrix.project.name }}.csproj `
        --no-restore `
        -c Release `
        -p:PackageVersion=${{ env.VERSION }}

    - name: Build package file
      shell: pwsh
      run: |
        dotnet pack ${{ matrix.project.name }}/${{ matrix.project.name }}.csproj `
          --no-build `
          -c Release -o ${{ env.OUTDIR }} `
          -p:PackageVersion=${{ env.VERSION }}

    - name: Upload built packages as artifacts
      uses: actions/upload-artifact@v7
      with:
        name: ${{ env.PACKAGES_ARTIFACT }}-${{ matrix.project.name }}
        path: |
          ${{ env.OUTDIR }}/*.nupkg
          ${{ env.OUTDIR }}/*.snupkg
        retention-days: 1

  upload-release-asset:
    needs: 
      - version
      - build-package
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      
    env:
      PACKAGE_DIR: 'packages'

    steps:
    - name: Download built packages artifacts
      uses: actions/download-artifact@v8
      with:
        path: ${{ env.PACKAGE_DIR }}
    - name: Upload to referenced GitHub release
      uses: shogo82148/actions-upload-release-asset@v1
      with:
        upload_url: ${{ github.event.release.upload_url }}
        asset_path: ${{ env.PACKAGE_DIR }}/**/*.nupkg

  publish-github-package:
    needs:
      - build-package
    runs-on: ubuntu-latest
            
    permissions:
      packages: write

    env:
      PACKAGE_DIR: 'packages'

    steps:
    - name: Download built packages artifacts
      uses: actions/download-artifact@v8
      with:
        path: ${{ env.PACKAGE_DIR }}

    - uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x

    - name: Push all to GitHub Packages
      run: |
        for f in ${{ env.PACKAGE_DIR }}/**/*.nupkg
        do
          echo "Pushing $f to GitHub Packages source..."
          dotnet nuget push "$f" \
            --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" \
            --api-key ${{ secrets.GITHUB_TOKEN }} \
            --no-symbols
        done

  publish-nuget-org-package:
    needs:
      - build-package
    runs-on: ubuntu-latest

    env:
      PACKAGE_DIR: 'packages'

    steps:
    - name: Download built packages artifacts
      uses: actions/download-artifact@v8
      with:
        path: ${{ env.PACKAGE_DIR }}

    - uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x

    - name: Push all to NuGet.org
      env:
        API_KEY: ${{ secrets.MATERIAL_THEMING_NUGET_KEY }}
      run: |
        for f in ${{ env.PACKAGE_DIR }}/**/*.nupkg
        do
          echo "Pushing $f to NuGet.org source..."
          dotnet nuget push "$f" \
            --source "nuget.org" \
            --api-key ${{ env.API_KEY }}
        done
